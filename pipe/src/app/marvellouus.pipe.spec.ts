import { MarvellouusPipe } from './marvellouus.pipe';

describe('MarvellouusPipe', () => {
  it('create an instance', () => {
    const pipe = new MarvellouusPipe();
    expect(pipe).toBeTruthy();
  });
});
